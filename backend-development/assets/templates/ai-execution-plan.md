# <迭代> S12 AI 执行计划

## 执行基线

- 项目规则：
- 工作包实现设计：
- 冻结协议：
- 用例矩阵：
- Review 规范与业务清单：

## 任务登记与依赖（唯一入口）

本表是当前迭代 `TASK-XX` 的唯一编号、工作包归属、前置依赖和状态入口。任务正文可以分文件维护，但不得再创建 `WP-00` 或其他重复任务表。

| 任务 | 工作包 | 责任单元/适配层 | 目标行为 | 前置任务 | 权威设计 | 文件白名单 | 失败验证命令 | 通过标准 | 回归与 Review | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## <TASK-XX> <任务名称>

- 目标与预期结果：
- 责任单元或调用适配层（只能有一个；微服务、单体模块/组件、分层适配器等均可）：
- 主入口或独立闭环（只能有一个；若合并必须说明不可分割原因）：
- 业务目的、下游消费者及 pending/outbox 等持久化状态语义：
- 入口、任务登记、实现和测试的逐项对应关系：
- 输入、异常、权限、并发和依赖失败边界：
- 允许修改：
- 禁止跨入的责任单元、仓库和文件：
- 禁止修改与禁止操作：
- Git、环境和外部沟通授权：
- 失败验证命令：
- 通过标准：
- 回归范围：
- Review 检查点：
- 人 Review 门禁：自 Review 完成后停止；明确通过后才提交并进入下一 TASK
- 阻塞、止损和断点续跑：
