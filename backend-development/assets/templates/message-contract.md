# <消息类型> 消息协议

## <事件名称>

- 交互编号：
- 通道：WS / MQ / IM / 其他
- 成熟度：
- 触发场景：
- 生产者：
- 消费者或接收人：
- Proto 权威路径：
- 公共对象文档：

## 载荷

| 字段路径 | Proto 类型 | 必填/规则 | 语义 | 兼容规则 |
| --- | --- | --- | --- | --- |

Proto message 结构是权威定义；本表只绑定字段路径与业务语义，不重复维护字段号。

## 传输与入库载体

| 载体 | 外层或数据表 | 复用的消息结构 | 独有字段 | 不包含 |
| --- | --- | --- | --- | --- |
| WebSocket/推送 | `<外层>` | `<消息体>` | `<填写>` | `<填写>` |
| 入库消息 | `<表>.<字段>` | `<复用的消息体或独立结构>` | `<填写>` | `<填写>` |

## 交付语义

- 路由或主题：
- 顺序与去重：
- 重试、死信或补偿：
- 消费幂等：
- 失败处理与调用方感知：
- 日志、指标和追踪标识：
- 示例：
