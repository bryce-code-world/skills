# <服务>-<业务> API 协议

## <接口名称>

- 交互编号：
- 成熟度：草案 / 待评审 / 已冻结 / 已废弃
- 调用方与场景：
- 方法与路由：
- 权限与身份：
- Proto 权威路径：
- 关联公共对象：

### 请求

```proto
// 直接引用或维护与冻结 source Proto 一致的请求结构；注释和校验写在字段旁。
message <Request> {
  // <字段语义与校验>
  <type> <field> = <number> [(validate.rules) = ...];
}
```

### 响应

```proto
message <Response> {
  <type> <field> = <number>;
}
```

公共请求/响应对象只在公共对象文档定义；这里通过类型名和链接复用，不重复展开字段。

### 错误与兼容

| 错误码或 reason | 条件 | 调用方处理 |
| --- | --- | --- |

- 幂等与并发：
- 批量、分页和排序：
- 兼容、废弃和替代路径：
- 示例与 Mock：
